{
  "version": 2,
  "buildCommand": "prisma generate --schema=backend/prisma/schema.prisma && prisma db push --schema=backend/prisma/schema.prisma --accept-data-loss && tsx backend/prisma/seed.ts && cd frontend && npm install && npm run build",
  "outputDirectory": "frontend/dist",
  "functions": {
    "api/index.ts": {
      "includeFiles": "backend/prisma/schema.prisma"
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.ts"
    },
    {
      "source": "/api",
      "destination": "/api/index.ts"
    },
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}
